# 📐 VISUAL ARCHITECTURE & FLOWCHARTS

---

## I. SYSTEM ARCHITECTURE DIAGRAM

```
┌─────────────────────────────────────────────────────────────────────┐
│                    SIGN LANGUAGE RECOGNITION SYSTEM                 │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│ Raw Videos  │
│(MP4, AVI)   │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ STEP 1: VIDEO PREPROCESSING                         │
│ preprocess_video.py                                 │
├──────────────────────────────────────────────────────┤
│ • Normalize FPS: → 30fps                           │
│ • Normalize Resolution: → 1280×720 (16:9)          │
│ • Motion Detection: Identify signing segments      │
│ • Pixel Normalization: [0,255] → [0,1]           │
│ • Output: Segmented videos (2-10s each)          │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ Preprocessed Videos (data/raw)                      │
│ └─ person/                                          │
│ └─ me/                                              │
│ └─ vietnam/                                         │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ STEP 2: KEYPOINT EXTRACTION                         │
│ video2npy.py                                        │
├──────────────────────────────────────────────────────┤
│ • Load: Each video frame                           │
│ • MediaPipe Holistic: Extract landmarks           │
│   ├─ Pose: 33 points × 3 coords = 99              │
│   ├─ Left Hand: 21 points × 3 = 63                │
│   ├─ Right Hand: 21 points × 3 = 63               │
│   └─ Total: 225 features/frame                    │
│ • Frame Sampling: Smart mode (remove idle frames)  │
│ • Output: (seq_len=64, 225) numpy arrays (.npy)   │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ Keypoint Sequences (data/npy)                       │
│ └─ person/file1.npy (64, 225)                      │
│ └─ person/file2.npy (64, 225)                      │
│ └─ me/file1.npy     (64, 225)                      │
│ └─ ...                                              │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ STEP 3: DATASET SPLIT                              │
│ split_dataset.py                                    │
├──────────────────────────────────────────────────────┤
│ • Stratified split: 70% train / 15% val / 15% test │
│ • Balanced class distribution                      │
│ • Output: 3 CSV files with indices                 │
└──────┬───────────────────────────────────────────────┘
       │
       ├─────────────────┬─────────────────┬──────────────┐
       ▼                 ▼                 ▼              ▼
    train.csv        val.csv           test.csv    (Optional)
    (70%)            (15%)             (15%)       Scaler
       │                 │                 │
       └─────────────────┴─────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────┐
│ STEP 4: DATA LOADING & AUGMENTATION                │
│ data_loader.py                                      │
├──────────────────────────────────────────────────────┤
│ • Load .npy files                                  │
│ • Normalize keypoints (center + scale)            │
│ • Runtime augmentation (train only):              │
│   ├─ Rotation: ±15°                               │
│   ├─ Scaling: ×0.85 to ×1.15                      │
│   ├─ Translation: ±8%                             │
│   ├─ Flip + Hand swap                             │
│   └─ Time masking                                 │
│ • Convert to PyTorch tensors                      │
│ • Batch: (batch_size=32, 64, 225)                │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ STEP 5: MODEL TRAINING                             │
│ train.py                                            │
├──────────────────────────────────────────────────────┤
│ Model: LSTM (or BiLSTM/GRU)                        │
│ Input: (batch, 64, 225) keypoint sequences        │
│ Architecture:                                      │
│   ├─ LSTM: 225 → 256 (2 layers)                   │
│   ├─ Dropout: 0.3                                 │
│   └─ Classifier: 256 → num_classes                │
│                                                    │
│ Loss: CrossEntropyLoss (label_smoothing=0.1)     │
│ Optimizer: AdamW (lr=1e-3, weight_decay=1e-4)    │
│ Scheduler: ReduceLROnPlateau (mode='max')        │
│ Early Stopping: patience=20                       │
│ Epochs: 100 (or early stop)                       │
│ Training Duration: ~1-5 hours (GPU)               │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ STEP 6: EVALUATION                                 │
│ eval.py                                            │
├──────────────────────────────────────────────────────┤
│ • Load best checkpoint                            │
│ • Evaluate on test set                            │
│ • Print metrics:                                  │
│   ├─ Accuracy                                     │
│   ├─ Precision / Recall / F1                      │
│   └─ Confusion matrix                             │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│ STEP 7: REALTIME INFERENCE                         │
│ infer_realtime.py                                   │
├──────────────────────────────────────────────────────┤
│ • Capture from webcam                             │
│ • Motion detection FSM                            │
│ • Extract keypoints per frame                     │
│ • Accumulate into sliding window                  │
│ • Model prediction when window full               │
│ • Display prediction on screen                    │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
  Prediction Output
  "người (0.92)"
```

---

## II. KEYPOINT EXTRACTION WORKFLOW

```
┌─────────────────────────────────────────────────────────────┐
│              VIDEO FRAME                                    │
│         (640×480 RGB format)                               │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ MediaPipe Holistic.process(image)                           │
└─────────────┬───────────────────────────────────────────────┘
              │
              ├──────────────────┬──────────────────┬──────────────────┐
              ▼                  ▼                  ▼                  ▼
        Pose Landmarks     Left Hand          Right Hand          Face (IGNORED)
        (33 points)        (21 points)        (21 points)         (468 points)
              │                  │                  │
              ├──────────────────┴──────────────────┤
              ▼
    ┌─────────────────────────────────────────┐
    │ Extract Coordinates (x, y, z)           │
    ├─────────────────────────────────────────┤
    │ Pose:       [0-32]   = 33×3 = 99 vals  │
    │ LeftHand:   [33-53]  = 21×3 = 63 vals  │
    │ RightHand:  [54-74]  = 21×3 = 63 vals  │
    │                                         │
    │ Total: 225-dimensional vector           │
    └─────────────┬───────────────────────────┘
                  │
                  ▼
    ┌─────────────────────────────────────────┐
    │ normalize_keypoints(seq)                │
    ├─────────────────────────────────────────┤
    │ 1. Reference point: midpoint of wrists │
    │ 2. Translation: center at origin       │
    │ 3. Scale: normalize by bbox diagonal   │
    │                                         │
    │ Formula:                                │
    │ L̂ = (L - L_ref) / ||L_max - L_min||   │
    └─────────────┬───────────────────────────┘
                  │
                  ▼
    ┌─────────────────────────────────────────┐
    │ OUTPUT: 225-dim feature vector         │
    │ Ready for model input                   │
    └─────────────────────────────────────────┘
```

---

## III. MODEL ARCHITECTURE DIAGRAM

```
INPUT: (batch_size=32, seq_len=64, input_dim=225)
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│                  LSTM Layer 1                               │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ Input: (32, 64, 225)                                    ││
│ │ LSTM(input_size=225, hidden_size=256, num_layers=2)    ││
│ │                                                         ││
│ │ Cell architecture:                                      ││
│ │   ├─ Input gate: i_t = σ(W_ii x_t + W_hi h_(t-1))     ││
│ │   ├─ Forget gate: f_t = σ(W_if x_t + W_hf h_(t-1))    ││
│ │   ├─ Cell state: c̃_t = tanh(W_ic x_t + W_hc h_(t-1))  ││
│ │   ├─ Cell update: c_t = f_t ⊙ c_(t-1) + i_t ⊙ c̃_t    ││
│ │   └─ Output gate: o_t = σ(W_io x_t + W_ho h_(t-1))    ││
│ │       h_t = o_t ⊙ tanh(c_t)                            ││
│ │                                                         ││
│ │ Output: (32, 64, 256)  [per-frame outputs]             ││
│ └──────────────────────────────────────────────────────────┘│
│                                                             │
│                  Layer 2 (same)                            │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ Output: (32, 64, 256)                                   ││
│ └──────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│              Take Last Hidden State                         │
│ out[:, -1, :] → (32, 256)                                  │
│ (Last frame contains full sequence information)            │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│                 Classification Head                        │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ Linear(256, 128) + ReLU                                 ││
│ │ Dropout(0.3)                                            ││
│ │ Linear(128, num_classes)                                ││
│ │                                                         ││
│ │ Output: (32, num_classes)  [logits]                     ││
│ └──────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│              Softmax + Argmax                              │
│ probs = softmax(logits)  → (32, num_classes)              │
│ pred = argmax(probs)     → (32,)  [class indices]         │
│ confidence = max(probs)  → (32,)  [confidences]           │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
OUTPUT: Class predictions + Confidences
```

---

## IV. TRAINING LOOP FLOWCHART

```
START
  │
  ▼
┌─────────────────────────────────────────┐
│ Load Configuration & Hyperparameters    │
│ device='cuda' or 'cpu'                  │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ Load Datasets                           │
│ train_ds, val_ds = SignLanguageDataset()│
│ label_map = train_ds.get_label_map()    │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ Build Model                             │
│ model = build_model(...)                │
│ model.to(device)                        │
└─────────────┬───────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ Create Optimizers                       │
│ optimizer = AdamW(lr=1e-3, ...)        │
│ scheduler = ReduceLROnPlateau(...)      │
│ criterion = CrossEntropyLoss(...)       │
└─────────────┬───────────────────────────┘
              │
              ▼
FOR epoch = 1 to max_epochs:
  │
  ├─────────────────────────────────────┐
  │     TRAINING PHASE                  │
  ├─────────────────────────────────────┤
  │ model.train()                       │
  │ for batch_idx, (X, y) in train_loader:
  │   ├─ X.to(device), y.to(device)    │
  │   ├─ logits = model(X)             │
  │   ├─ loss = criterion(logits, y)   │
  │   │                                 │
  │   ├─ Check NaN                      │
  │   ├─ optimizer.zero_grad()          │
  │   ├─ loss.backward()                │
  │   ├─ clip_grad_norm_(max=1.0)      │
  │   └─ optimizer.step()               │
  │                                     │
  │ avg_train_loss = mean(losses)       │
  │ train_acc = accuracy(preds, labels) │
  └─────────────┬───────────────────────┘
                │
                ▼
  ├─────────────────────────────────────┐
  │   VALIDATION PHASE                  │
  ├─────────────────────────────────────┤
  │ model.eval()                        │
  │ with torch.no_grad():               │
  │   for X, y in val_loader:           │
  │     ├─ logits = model(X)            │
  │     ├─ loss = criterion(...)        │
  │     └─ collect preds & labels       │
  │                                     │
  │ avg_val_loss = mean(losses)         │
  │ val_acc = accuracy(preds, labels)   │
  └─────────────┬───────────────────────┘
                │
                ▼
  ├─────────────────────────────────────┐
  │  UPDATE SCHEDULER                   │
  ├─────────────────────────────────────┤
  │ scheduler.step(val_acc)             │
  │ current_lr = optimizer.param_groups │
  │                                     │
  │ Print epoch results                 │
  └─────────────┬───────────────────────┘
                │
                ▼
  ├─────────────────────────────────────┐
  │  CHECK BEST MODEL                   │
  ├─────────────────────────────────────┤
  │ if val_acc > best_val_acc:          │
  │   ├─ best_val_acc = val_acc         │
  │   ├─ save_checkpoint(...)           │
  │   └─ log("New best!")               │
  └─────────────┬───────────────────────┘
                │
                ▼
  ├─────────────────────────────────────┐
  │  CHECK EARLY STOPPING               │
  ├─────────────────────────────────────┤
  │ if early_stop(val_acc):             │
  │   └─ BREAK                          │
  └─────────────┬───────────────────────┘
                │
                └─ Continue to next epoch
                  
  (After all epochs or early stop)
  │
  ▼
┌─────────────────────────────────────┐
│ TRAINING COMPLETE                   │
│ - Load best checkpoint              │
│ - Evaluate on test set              │
│ - Print final metrics               │
│ - Save training history             │
└─────────────────────────────────────┘
  │
  ▼
END
```

---

## V. REALTIME INFERENCE FSM

```
┌──────────────────────────────────────────────────────────┐
│                 REALTIME INFERENCE                       │
│         Finite State Machine (FSM) Diagram               │
└──────────────────────────────────────────────────────────┘

                    START
                      │
                      ▼
           ┌──────────────────────┐
           │ Load Model + Weights │
           │ Load Label Map       │
           │ Init Webcam          │
           │ Init MediaPipe       │
           └──────────┬───────────┘
                      │
                      ▼
        ┌─────────────────────────────────┐
        │  STATE = "waiting"              │
        │  Waiting for sign to start      │
        │                                 │
        │  Display: "Waiting for sign..."│
        │                                 │
        │  Motion Threshold: LOW          │
        └──────────┬──────────────────────┘
                   │
        ┌──────────▼──────────┐
        │ Motion Detected?    │
        └──────────┬──────────┘
                   │
            ┌──────┴──────┐
            │ NO          │ YES
            │             ▼
            │      ┌──────────────────────┐
            │      │ STATE = "recording"  │
            │      │ Recording started    │
            │      │                      │
            │      │ Display: "Recording" │
            │      │ segment = []         │
            │      │ still_count = 0      │
            │      └──────────┬───────────┘
            │                 │
            │                 ▼
            │      ┌──────────────────────────┐
            │      │ Extract Keypoints       │
            │      │ vec = extract_keypoints()
            │      │ segment.append(vec)     │
            │      │                         │
            │      │ Motion Still Detected?  │
            │      └──────────┬───────────────┘
            │                 │
            │          ┌──────┴──────┐
            │          │ YES  │ NO   │
            │          │      ▼      │
            │          │   still_count=0
            │          │             │
            │          └──────┬──────┘
            │                 │
            │                 ▼
            │      ┌──────────────────────────┐
            │      │ still_count >= 8?        │
            │      │ (motion stopped for 8fr) │
            │      └──────────┬───────────────┘
            │                 │
            │          ┌──────┴──────┐
            │          │ NO   │ YES  │
            │          │      ▼      │
            │          │  ┌─────────────────────┐
            │          │  │ PROCESS SEGMENT    │
            │          │  │ • Pad to seq_len  │
            │          │  │ • Forward pass     │
            │          │  │ • Get prediction   │
            │          │  │ • Save last_pred   │
            │          │  │ • Display on frame │
            │          │  └──────────┬─────────┘
            │          │             │
            │          │             ▼
            │          └────────┬────────────────┐
            │                   ▼                │
            │         Reset:                     │
            │         segment = []               │
            │         still_count = 0            │
            │         STATE = "waiting"          │
            │                                    │
            └────────────────┬───────────────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Display Last Pred    │
                  │ if time < 1.5s       │
                  │                      │
                  │ Capture Exit Signal  │
                  │ (Press 'q')          │
                  └──────────┬───────────┘
                             │
                      ┌──────▼──────┐
                      │ Exit?       │
                      └──────┬──────┘
                             │
                        ┌────┴────┐
                        │ NO   YES│
                        │         ▼
                        │      CLEANUP
                        │      Release Resources
                        │      END
                        │
                        └─────────┘
```

---

## VI. DATA AUGMENTATION PIPELINE

```
Original Keypoint Sequence
(64, 225) shape
      │
      ▼
┌──────────────────────────────────┐
│ 1. RANDOM ROTATION               │
│    angle ~ U(-15°, 15°)          │
│                                  │
│    [cos(θ)  -sin(θ)]             │
│ R = [sin(θ)   cos(θ)]            │
│                                  │
│    Apply to (x,y) coordinates   │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ 2. RANDOM SCALING                │
│    scale ~ U(0.85, 1.15)         │
│    x' = x * scale                │
│    y' = y * scale                │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ 3. RANDOM TRANSLATION            │
│    shift ~ U(-0.08, 0.08) × 2    │
│    x' = x + shift_x              │
│    y' = y + shift_y              │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ 4. HORIZONTAL FLIP (50% prob)    │
│    x' = -x (mirror)              │
│    Swap: left_hand ↔ right_hand  │
│                                  │
│ (Pose: 0-32)                    │
│ (LeftHand: 33-53)  }  swap      │
│ (RightHand: 54-74) }            │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ 5. TIME MASKING (20% prob)       │
│    Random frames → zeros         │
│    mask_len ~ U(1, 4)            │
│    start ~ U(0, seq_len-mask)   │
│    arr[start:start+len] = 0     │
└──────────┬───────────────────────┘
           │
           ▼
Augmented Sequence (ready for training)
```

---

## VII. FRAME SAMPLING COMPARISON

```
VIDEO WITH 120 FRAMES
(Frames 0 to 119)

┌─────────────────────────────────────────────────────────┐
│ Mode 1: Uniform Sampling (Traditional)                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Divide 120 frames into 10 chunks:                      │
│ [0-11] [12-23] [24-35] [36-47] [48-59]               │
│ [60-71] [72-83] [84-95] [96-107] [108-119]           │
│                                                         │
│ Sample from each chunk:                               │
│ Chunk 0,1: last frame     → frames 11, 23            │
│ Chunk 2-7: middle frame   → frames 29, 41, 53, ...  │
│ Chunk 8,9: first frame    → frames 96, 108          │
│                                                         │
│ Selected: [11, 23, 29, 41, 53, 65, 77, 89, 96, 108] │
│                                                         │
│ Problem: Includes frames 0-11 which may have no motion│
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Mode 2: Smart Sampling (USED BY DEFAULT)              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Step 1: Pre-sample into 12 chunks (remove idle)       │
│ [0-9] [10-19] [20-29] [30-39] [40-49] [50-59]        │
│ [60-69] [70-79] [80-89] [90-99] [100-109] [110-119]  │
│                                                         │
│ Step 2: Remove first + last chunk (usually idle)      │
│ Keep middle 10 chunks: [10-109] (100 frames)         │
│                                                         │
│ Step 3: Divide 100 frames into 10 chunks:            │
│ [10-19] [20-29] [30-39] [40-49] [50-59]             │
│ [60-69] [70-79] [80-89] [90-99] [100-109]           │
│                                                         │
│ Step 4: Apply mode 1 sampling                        │
│ Selected: [19, 29, 35, 45, 55, 65, 75, 85, 95, 105]│
│                                                         │
│ Benefit: More focused on motion regions              │
└─────────────────────────────────────────────────────────┘
```

---

## VIII. COORDINATE NORMALIZATION VISUALIZATION

```
BEFORE NORMALIZATION:
┌─────────────────────────────────────────┐
│  Frame with scattered keypoints        │
│  (absolute pixel coordinates)           │
│                                        │
│      ★ (200, 100) left hand           │
│        |                               │
│        | wrist ref points              │
│      ◆ (250, 120)  right hand         │
│        |                               │
│      ● (225, 150) pose landmarks       │
│        |                               │
│      ■ (100, 180)                     │
│                                        │
│  Problem: Position/scale dependent    │
│  Different people → different coords   │
└─────────────────────────────────────────┘

STEP 1: TRANSLATION (Center at Wrist)
┌─────────────────────────────────────────┐
│  Compute wrist reference point:         │
│  L_ref = (L_wrist + R_wrist) / 2      │
│       = ((200+250)/2, (100+120)/2)     │
│       = (225, 110)                     │
│                                        │
│  Translate all points:                │
│  x' = x - 225                         │
│  y' = y - 110                         │
│                                        │
│  Result: ◆ (-25, 10)                  │
│         ★ (-25, -10)                  │
│         ● (0, 40)                     │
│         ■ (-125, 70)                  │
│                                        │
│  Now centered at origin                │
└─────────────────────────────────────────┘

STEP 2: SCALING (Normalize by Bounding Box)
┌─────────────────────────────────────────┐
│  Find bounding box:                    │
│  min = (-125, -10)                     │
│  max = (0, 70)                         │
│  diagonal = ||(0-(-125), 70-(-10))||   │
│           = ||(125, 80)||              │
│           = √(125² + 80²) = 148.66    │
│                                        │
│  Scale all coordinates:                │
│  x'' = x' / 148.66                    │
│  y'' = y' / 148.66                    │
│                                        │
│  Result: ◆ (-0.168, 0.067)            │
│         ★ (-0.168, -0.067)            │
│         ● (0, 0.269)                  │
│         ■ (-0.840, 0.470)             │
│                                        │
│  All coordinates in [-1, 1] range      │
└─────────────────────────────────────────┘

BENEFIT:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Position Invariant: Works regardless of person position
✓ Scale Invariant: Works for different hand sizes
✓ Robust: Model learns pose shapes, not absolute positions
✓ Better Generalization: Fewer parameters needed
```

---

End of Visual Diagrams

